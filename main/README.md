# FTO Alg Finder

Rust workspace for building an optimal Face-Turning Octahedron solver.

## Crates

- `fto-core`: puzzle state, move tables, pruning tables, and search logic.
- `fto-cli`: command-line entry point for solver and tooling workflows.

Reference implementations live in `../trivials`, which is intentionally ignored by git.
